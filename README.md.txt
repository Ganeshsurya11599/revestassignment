-- revestbackend
	* npm install
	* install a PostgreSQL and create a server in pgadmin
	* run the code (node index.js)
	* default admin user creation (http://localhost:8080/adduserdata/{password});
-- revestlogin
	*npm install
	*run the code (ng serve)